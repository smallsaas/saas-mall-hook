import useTokenRequest from 'zero-element-boot/lib/components/hooks/useTokenRequest'

export default function useApiMallOrder({}){
    const api = ''

    /**
     * 查询订单列表
     * @param {} data 
     */
    function query(data){
    }

    function get({id, ...data}){
    }

    function set(data){
    }

    

    return []
}